init:
  - git config --global core.autocrlf input
build_script:
  - ps: .\build.ps1 -Target CI
test: off  
artifacts:
  - path: '.build\**\testResults\*Test*.xml'
    name: testResults

  - path: '.build\**\*.nupkg'
    name: packages
